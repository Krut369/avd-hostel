[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
  DISABLE_NEXTJS_BUILD = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
